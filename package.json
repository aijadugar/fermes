{
  "name": "farming-agent-backend",
  "version": "0.1.0",
  "description": "Backend agent API: Sarvam (STT/LLM/TTS) + Mireye (site suitability) + Places (nearby businesses). Frontend to be attached separately (e.g. three.js).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
